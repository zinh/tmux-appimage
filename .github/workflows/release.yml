on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    name: build tmux app image
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: build docker image
        run: |
          docker build . -t tmux
